CREATE TABLE IF NOT EXISTS telegram_config (id BIGINT AUTO_INCREMENT PRIMARY KEY, bot_token VARCHAR(100) NOT NULL, chat_id VARCHAR(50) NOT NULL, enabled BOOLEAN NOT NULL DEFAULT TRUE, description VARCHAR(255), created_at DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP, updated_at DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP);
